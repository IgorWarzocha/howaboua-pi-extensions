{
  "name": "pi-todos",
  "version": "0.1.0",
  "description": "Todo manager extension",
  "keywords": ["pi-extension"],
  "type": "module",
  "scripts": {
    "build": "echo '[WARNING] Built artifacts MUST NOT be read; verify only file length/size.' && bun build ./index.ts --target=node --format=esm --outfile ./dist/index.js --external @mariozechner/pi-ai --external @mariozechner/pi-coding-agent --external @mariozechner/pi-tui && echo '[OK] Build finished. DO NOT read dist/index.js; run: wc -l dist/index.js && ls -lh dist/index.js'",
    "check:types": "bunx tsc --noEmit --skipLibCheck",
    "check:format": "oxfmt --check .",
    "format": "oxfmt .",
    "check": "bun run check:types && bun run check:format"
  },
  "peerDependencies": {
    "@mariozechner/pi-ai": "*",
    "@mariozechner/pi-coding-agent": "*",
    "@mariozechner/pi-tui": "*"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
